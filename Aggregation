-- Data Transformation Example: Aggregating sales by region
SELECT region, SUM(sales_amount) AS total_sales
FROM sales_data
WHERE sale_date BETWEEN '2025-01-01' AND '2025-01-31'
GROUP BY region
ORDER BY total_sales DESC;
